using System;

namespace URI
{
    class URI
    {
        static void Main()
        {
            double km = double.Parse(Console.ReadLine());

            km = km * 2;

            Console.WriteLine($"{km} minutos");
        }
    }
}